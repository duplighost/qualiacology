# CLAUDE.md

Read **AGENTS.md** in this directory before changing anything — it is the
canonical, kept-current entry point for this site: the always-on rules, the
ship checklist, and a task table routing to the detailed guides in
`build/docs/` (read only the guides your task touches). Everything there
applies to you. The two headlines: **merging to `main` deploys the live site, so use a
feature branch, pull request, and verified Deploy Preview first**, and **all
copy is Alex's own voice — never invent taglines**. Never push or merge a
production change without Alex's explicit approval.

Claude-specific notes:

- `HANDOFF.md` here is a local, git-ignored session diary (it never reaches
  GitHub). Read it for recent-session context; append a changelog entry when
  you finish substantial work.
- Persistent memory (`qualiacology-site` and related files) should point here,
  not duplicate this content. If the workflow changes, update AGENTS.md in the
  same commit — that's the single source of truth for every agent, including
  non-Claude ones.
