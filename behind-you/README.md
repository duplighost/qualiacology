# Behind You

Open `index.html` in a browser to play locally.

For static hosting, upload this whole folder. The game uses the local files in `vendor/`, so the runtime libraries are included with the package.
