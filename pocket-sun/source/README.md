# Pocket Sun source

The canonical local source is `C:\Users\Alex\Projects\pocket-sun`. This folder preserves the editable game with its site release. Run `npm install`, then `npm run build`; output is `dist/site`, served at `/pocket-sun/`. `index.html.template` becomes the Vite entry during the build. The source snapshot itself is not a second playable site route.
