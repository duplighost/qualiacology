{"web-source/app/page.tsx":{"original_sha256":"30bcd41acf03ba2ccd52a17c11166bf415ebe6f425c7ca6fce50e4488b063447","patched_sha256":"e7804951debbe2ad61a8df1f491121de922a4d52f91fcf2c2ca7f52931dcdde7","edits":[[149,149,"import { createGlowCache, strokeBloom, createTextSprite, type TextSprite } from \"./render-cache\";\n"],[20210,20210,"    const glowCache = createGlowCache();\n    const floaterSprites = new WeakMap<Floater, TextSprite>();\n"],[21005,21005,"    let lastHudSignature = \"\";\n"],[21110,21157,"    let renderQuality: RenderQuality = \"balanced\";\n"],[21834,21857,"      quality: \"balanced\",\n"],[26105,26172,"      if (elapsed < 1000 || performanceFrames.length < 8) return;\n"],[27366,27394,"      // Canvas commands may return quickly while raster/compositing misses frames.\n      // Judge the delivered frame cadence, not just JS command-submission time.\n      const slowDelivery = averageFrameMs > 20.5 || percentile(performanceFrames, .95) > 30;\n      const fastDelivery = averageFrameMs < 18.5 && percentile(performanceFrames, .95) < 24;\n      if (p95DrawMs > 11 || slowDelivery) {\n"],[27459,27495,"      } else if (p95DrawMs < 5.5 && fastDelivery) {\n"],[27647,27717,"      if (qualitySlowWindows >= 2 && renderQuality !== \"recovery\" && now - qualityChangedAt > 3000) {\n"],[27911,28016,"      } else if (qualityFastWindows >= 10 && renderQuality !== \"high\" && now - qualityChangedAt > 10000) {\n"],[40093,40193,"      atmosphereScale = renderQuality === \"high\" ? 0.7 : renderQuality === \"balanced\" ? 0.55 : 0.4;\n"],[65411,65476,"      const path = new Path2D();\n      path.moveTo(tail.x, tail.y);\n"],[65651,65813,"        path.quadraticCurveTo(point.x, point.y, (point.x + next.x) * 0.5, (point.y + next.y) * 0.5);\n      }\n      path.lineTo(state.star.x, state.star.y);\n      return path;\n"],[66201,66548,"      glowCache.draw(context, \"sun\", \"\", radius * 7);\n"],[69688,69924,"      // Only the slow-moving nebula/starfield is throttled. The moon, sun,\n      // objects, gestures and particles still render at the foreground cadence.\n      const atmosphereFps = renderQuality === \"high\" ? 12 : renderQuality === \"balanced\" ? 10 : 8;\n"],[71297,71329,"        context.shadowBlur = 0;\n"],[71616,71642,"        strokeBloom(context, 8);\n"],[72266,72668,"        glowCache.draw(context, \"gate\", color, gate.r * 2.15);\n"],[72778,72824,"        context.shadowBlur = 0;\n"],[73260,73288,"          strokeBloom(context, active ? 20 : 9);\n"],[75844,76295,"        glowCache.draw(context, \"bumper\", color, bumper.r * 2.3 * pulse);\n"],[78520,78881,""],[78916,79060,"        glowCache.draw(context, mote.kind, color, mote.r * (mote.kind === \"prism\" ? 4.6 : 3.6) * breathe);\n"],[81757,81790,"        context.shadowBlur = 0;\n"],[82037,82063,"        strokeBloom(context, 16);\n"],[83059,83059,"        const trailPath = traceTrail()!;\n"],[83752,83821,"        context.shadowBlur = 0;\n"],[83957,84005,"        strokeBloom(context, renderQuality === \"recovery\" ? 11 : 20, trailPath);\n"],[84174,84222,"        context.stroke(trailPath);\n"],[84657,84705,"        context.stroke(trailPath);\n"],[85992,86033,"        context.shadowBlur = 0;\n"],[86183,86209,"        strokeBloom(context, 10 * alpha);\n"],[86989,87442,"        let sprite = floaterSprites.get(floater);\n        if (!sprite) {\n          sprite = createTextSprite(floater.text, floater.size, floater.color, alphaColor(colors.deep, 0.86), state.dpr);\n          floaterSprites.set(floater, sprite);\n        }\n        const drawnWidth = sprite.textWidth * pop + sprite.lineWidth * 2;\n"],[87749,87922,"        context.drawImage(sprite.image, -sprite.width * .5, -sprite.height * .5, sprite.width, sprite.height);\n"],[94387,94387,"      const signature = `${Math.round(state.score)}:${state.paused}:${state.paletteIndex}:${state.fever > 0}`;\n      if (force || signature !== lastHudSignature) {\n      lastHudSignature = signature;\n"],[94821,94821,"      }\n"],[113838,113838,"    const renderInterval = 1000 / 60;\n    let renderDeadline = lastTime;\n    let lastPresentedAt = lastTime;\n    let wasRenderingActive = false;\n"],[114067,114238,"        // Never throttle pointer events or simulation to save rendering work.\n        if (!wasRenderingActive) {\n          renderDeadline = now;\n          lastPresentedAt = now - renderInterval;\n          wasRenderingActive = true;\n        }\n        if (now + .5 >= renderDeadline) {\n          const drawStartedAt = performance.now();\n          draw();\n          recordPerformance(now, now - lastPresentedAt, performance.now() - drawStartedAt);\n          lastPresentedAt = now;\n          // Advance a stable deadline. Missed frames do not trigger catch-up draws.\n          renderDeadline += renderInterval;\n          if (renderDeadline < now - .5) renderDeadline = now + renderInterval;\n          refreshHud();\n        }\n      } else wasRenderingActive = false;\n"],[115529,115529,"      glowCache.clear();\n"]]},"web-source/app/cinematic.ts":{"original_sha256":"1d6d961116b5d2f5629dd5d0cc93f2bf879beaa4c814f2081686902b523bc1d3","patched_sha256":"fe06f4e8dd37e9d64d53d2b56bc6cdd7123213125f3b5c0d5c107c71bcb3f7c2","edits":[[423,423,"const backgroundStars = Array.from({ length: 270 }, (_, i) => {\n  const depth = hash(i, 71);\n  return { depth, x: hash(i, 3), y: hash(i, 9), radius: depth > .985 ? 1.35 : .25 + depth * .58,\n    color: i % 7 === 0 ? \"#eebc91\" : \"#c6deee\" };\n});\n"],[6300,6585,"  const drift = Math.sin(time * .018) * 12;\n  for (let i = 0; i < backgroundStars.length; i++) {\n    const star = backgroundStars[i], { depth, radius } = star;\n    const x = (star.x * width + drift * depth + width) % width;\n    const y = (star.y * height + time * depth * .3) % height;\n"],[6660,6717,"    ctx.fillStyle = star.color;\n"]]},"web-source/app/render-cache.ts":{"original_sha256":"e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855","patched_sha256":"e28c3ed37b88f1fe66c3a39284c00e1e4517ece9a3bc016937626221523c77d6","edits":[[0,0,"/** Rendering-only caches. No gameplay RNG, collision, gesture or score state. */\nexport type GlowKind = \"sun\" | \"spark\" | \"prism\" | \"gate\" | \"bumper\";\nconst gradientStops: Record<Exclude<GlowKind, \"sun\">, [number, number][]> = {\n  spark: [[0, .46], [.28, .2], [1, 0]],\n  prism: [[0, .72], [.28, .2], [1, 0]],\n  gate: [[0, .6], [.35, .22], [1, 0]],\n  bumper: [[0, .22], [.35, .12], [1, 0]],\n};\n/** Only the soft halo is quantized; the sharp object keeps its exact palette. */\nfunction glowColor(color: string): [string, string] {\n  let rgb: number[];\n  if (color.startsWith(\"#\")) {\n    const value = color.slice(1);\n    rgb = value.length === 3 ? [...value].map(c => parseInt(c + c, 16))\n      : [0, 2, 4].map(i => parseInt(value.slice(i, i + 2), 16));\n  } else rgb = (color.match(/[\\d.]+/g) || [\"255\", \"255\", \"255\"]).slice(0, 3).map(Number);\n  const quantized = rgb.map(v => Math.max(0, Math.min(255, Math.round(v / 16) * 16)));\n  return [quantized.join(\",\"), quantized.join(\" \")];\n}\nexport function createGlowCache() {\n  const cache = new Map<string, HTMLCanvasElement>();\n  // 96 RGBA sprites at 128x128: at most 6 MiB before browser overhead.\n  const limit = 96;\n  function get(kind: GlowKind, color: string) {\n    const [keyColor, rgb] = kind === \"sun\" ? [\"sun\", \"\"] : glowColor(color);\n    const key = kind + \":\" + keyColor;\n    let sprite = cache.get(key);\n    if (sprite) { cache.delete(key); cache.set(key, sprite); return sprite; }\n    sprite = document.createElement(\"canvas\");\n    sprite.width = sprite.height = kind === \"sun\" ? 256 : 128;\n    const ctx = sprite.getContext(\"2d\")!;\n    const r = sprite.width / 2;\n    const g = ctx.createRadialGradient(r, r, kind === \"sun\" ? r * .6 / 7 : kind === \"bumper\" ? r * .2 / 2.3 : 0, r, r, r);\n    if (kind === \"sun\") {\n      g.addColorStop(0, \"#ffb53caa\"); g.addColorStop(.18, \"#ff8a263e\");\n      g.addColorStop(.48, \"#ff772812\"); g.addColorStop(1, \"#ff772800\");\n    } else for (const [offset, opacity] of gradientStops[kind]) g.addColorStop(offset, `rgb(${rgb} / ${opacity})`);\n    ctx.fillStyle = g; ctx.fillRect(0, 0, sprite.width, sprite.height);\n    if (cache.size >= limit) cache.delete(cache.keys().next().value!);\n    cache.set(key, sprite);\n    return sprite;\n  }\n  return {\n    draw(ctx: CanvasRenderingContext2D, kind: GlowKind, color: string, radius: number) {\n      const sprite = get(kind, color);\n      ctx.drawImage(sprite, -radius, -radius, radius * 2, radius * 2);\n    },\n    clear() { cache.clear(); },\n    get size() { return cache.size; },\n  };\n}\n/** Soft additive halo without asking Canvas to blur a moving path every frame.\n * The caller owns the path and composite mode; all modified state is restored.\n */\nexport function strokeBloom(ctx: CanvasRenderingContext2D, blur: number, path?: Path2D) {\n  const alpha = ctx.globalAlpha, width = ctx.lineWidth;\n  const stroke = () => path ? ctx.stroke(path) : ctx.stroke();\n  ctx.globalAlpha = alpha * .075; ctx.lineWidth = width + blur * 1.3; stroke();\n  ctx.globalAlpha = alpha * .16; ctx.lineWidth = width + blur * .55; stroke();\n  ctx.globalAlpha = alpha; ctx.lineWidth = width; stroke();\n}\nexport type TextSprite = { image: HTMLCanvasElement; width: number; height: number; textWidth: number; lineWidth: number };\n/** A floater's text/outline/glow are immutable; bake them once, animate the image. */\nexport function createTextSprite(text: string, size: number, color: string, outline: string, dpr: number): TextSprite {\n  const image = document.createElement(\"canvas\");\n  const ctx = image.getContext(\"2d\")!;\n  const font = `900 ${size}px 'Arial Black', 'Avenir Next', ui-sans-serif, sans-serif`;\n  ctx.font = font;\n  const textWidth = ctx.measureText(text).width, lineWidth = Math.max(2.2, size * .16);\n  const pad = 30;\n  const width = Math.ceil(textWidth + pad * 2), height = Math.ceil(size * 1.5 + pad * 2);\n  image.width = Math.ceil(width * dpr); image.height = Math.ceil(height * dpr);\n  ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n  ctx.font = font; ctx.textAlign = \"center\"; ctx.textBaseline = \"middle\";\n  ctx.fillStyle = color; ctx.strokeStyle = outline; ctx.lineWidth = lineWidth;\n  ctx.shadowColor = color; ctx.shadowBlur = 12;\n  ctx.strokeText(text, width / 2, height / 2); ctx.fillText(text, width / 2, height / 2);\n  return { image, width, height, textWidth, lineWidth };\n}\n"]]},"app/build.gradle":{"original_sha256":"3e917b7370ac427985d32ed6edbdc75e0bd7c47d79c345fb770f931b34ff5393","patched_sha256":"f9f7fbf206b73b17a225e3a43e4973bc09e96725f8ab4475aed6e175246630fb","edits":[[222,288,"        versionCode 3020002\n        versionName '3.2.0-android.2'\n"]]}}